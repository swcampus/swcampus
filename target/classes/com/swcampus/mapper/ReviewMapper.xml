<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swcampus.mapper.ReviewMapper">

	<!-- 리뷰 등록 -->
	<insert id="insert">
	
		<selectKey keyProperty="reviewId" order="BEFORE" resultType="Long">
			select seq_review.nextval from dual
		</selectKey>
	
		insert into review(reviewId, email, lectureId, starRating, regDate, contents)
			values(#{reviewId}, #{email}, #{lectureId}, #{starRating}, sysdate, #{contents})
	</insert>
	
	<!-- 전체 리뷰 -->
	<select id="getAllList" resultType="com.swcampus.domain.ReviewVO">
		select * from review order by reviewId desc
	</select>
	
	<!-- 특정 강좌에 해당하는 리뷰 목록 -->
	<select id="getListByLectureId" resultType="com.swcampus.domain.ReviewVO">
		select * from review where lectureId = #{lectureId} order by reviewId desc
	</select>
	
	<!-- 특정 회원이 작성한 리뷰 목록 -->
	<select id="getMyList" resultType="com.swcampus.domain.ReviewVO">
		select * from review where email = #{email} order by reviewId desc
	</select>
	
	<!-- 리뷰 상세 -->
	<select id="read" resultType="com.swcampus.domain.ReviewVO">
		select * from review where reviewId = #{reviewId}
	</select>
	
	<!-- 리뷰 수정 -->
	<update id="update">
		update review 
			set lectureId = #{lectureId},
				starRating = #{starRating},
				contents = #{contents}
			where reviewId = #{reviewId}	
	</update>
	
	<!-- 리뷰 삭제 -->
	<delete id="delete">
		delete from review where reviewId = #{reviewId}
	</delete>
	
	<!-- 베스트 강의 리뷰 8개 목록 -->
	<select id="getBestList">
		<![CDATA[
		select * 
			from (
				select lectureId, AVG(NVL(starRating, 0)) AS avg 
					from review 
					group by lectureId 
					order by AVG desc
				)
    		where rownum <= 8
    	]]>
	</select>
	
	<!-- 최신 강의 리뷰 8개 목록 -->
	<select id="getNewsetList"></select>
	
	<!-- 전체 리뷰 개수 -->
	
	
	<!-- 특정 강의의 리뷰 개수 -->
	
</mapper>